Fase 3: entrega confiable de reportes

Nuevo send_ledger.py: registro persistente (reporte, fecha) con semantica
claim/confirm/release — un reporte se envia EXACTAMENTE una vez aunque el
job se dispare N veces (retry ambiguo, doble capa, re-enable accidental,
catch-up). Claims huerfanos expiran a los 30 min.

teams_bot.py:
- _reliable_job: wrapper universal ledger + 3 reintentos + alerta por
  correo a Mateo al agotar (antes solo morning_sales lo tenia; los recaps
  mensuales no tenian NI try/except — S4).
- Jobs envueltos: morning_sales, weekly_summaries, consolidated_daily,
  monthly recaps x2, logistics.
- misfire_grace_time=3600 + coalesce (antes 1s: un deploy a la hora del
  job perdia la ejecucion — S2) + catch-up al arrancar via ledger (con
  seed en el primer arranque para no duplicar lo pre-ledger).
- Lease de instancia unica en el storage compartido del App Service: si
  escala a mas de 1 instancia solo una corre el scheduler (S3).
- logistics_morning migrado al bot, gateado por LOGISTICS_IN_BOT=1 para
  el cutover coordinado con el azfunc (S5).
- El endpoint health expone ledger del dia + lease (observabilidad).
- safe_json.on_corruption conectado a alerta por correo.

daily_report.py (R1): _safe registra fallos; fuentes CRITICAS caidas =>
el correo NO sale (RuntimeError -> retry+alerta del bot) en vez de enviar
"Vendimos $0" con exit 0; fuentes secundarias => banner de Datos parciales.

reply_agent + apollo_rest (C2/C3): ApolloAPIError distingue outage de
no-es-prospecto; JSON invalido de Claude marca parse_error — ninguno de
los dos marca mas el correo como procesado-para-siempre.

monthly_recap (R3): fix crash pace_pct=None.

Tests (13 nuevos, 45 total): nunca-dos-veces (incluye claim concurrente),
horarios/dias exactos de cada job, catch-up respeta calendario, retry con
recuperacion, alerta al agotar, reporte critico no sale con $0.
