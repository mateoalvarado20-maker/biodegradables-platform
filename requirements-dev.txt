# Dependencias de desarrollo y CI (no van a ningún deploy).
# Instalar con: pip install -r requirements-dev.txt -r requirements_bot.txt
pytest>=9.0
ruff>=0.15
# pyyaml también está en los requirements de deploy (core_config puede leer
# tenants/<slug>/config.yaml si TENANT_CONFIG_SOURCE=yaml).
