# Dependencias de desarrollo y CI (no van a ningún deploy).
# Instalar con: pip install -r requirements-dev.txt -r requirements_bot.txt
pytest>=9.0
ruff>=0.15
# PyYAML: usado por core/config/loader.py (carga de tenants/<slug>/config.yaml).
# Vive en dev/CI porque el núcleo multiempresa aún NO está cableado al deploy del
# bot; cuando se cablee, mover a requirements.txt / requirements_bot.txt.
pyyaml>=6.0
